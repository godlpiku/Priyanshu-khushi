# Sample workflow for building and deploying a Jekyll site to GitHub Pages
name: Deploy Jekyll with GitHub Pages dependencies preinstalled

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For My Khushi ❤️</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
body {
    margin: 0;
    font-family: 'Poppins', sans-serif;
    background: linear-gradient(to bottom, #ffe6f0, #fff);
    text-align: center;
    overflow-x: hidden;
}

.container {
    padding: 30px 15px;
    position: relative;
    z-index: 2;
}

h1 {
    color: #ff2f6d;
    font-size: 2.3em;
}

p {
    color: #444;
    font-size: 1.1em;
    line-height: 1.6;
    max-width: 600px;
    margin: auto;
}

.couple img {
    width: 260px;
    margin: 25px 0;
    border-radius: 20px;
}

.flowers {
    font-size: 30px;
    animation: float 4s infinite ease-in-out;
}

@keyframes float {
    0% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
    100% { transform: translateY(0); }
}

/* ❤️ Buttons */
button {
    margin: 12px;
    padding: 12px 25px;
    font-size: 16px;
    background: #ff2f6d;
    color: white;
    border: none;
    border-radius: 25px;
    cursor: pointer;
    box-shadow: 0 6px 15px rgba(0,0,0,0.2);
}

/* 💚 WhatsApp Button */
.whatsapp {
    background: #25D366;
}

/* 💖 Popup */
.popup {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.6);
    justify-content: center;
    align-items: center;
    z-index: 5;
}

.popup-content {
    background: white;
    padding: 25px;
    border-radius: 20px;
    max-width: 300px;
    text-align: center;
}

.popup-content h2 {
    color: #ff2f6d;
}

/* 💕 Heart Rain */
.heart {
    position: fixed;
    top: -10px;
    font-size: 20px;
    color: #ff2f6d;
    animation: fall linear infinite;
    z-index: 1;
}

@keyframes fall {
    0% { transform: translateY(0); opacity: 1; }
    100% { transform: translateY(110vh); opacity: 0; }
}

.footer {
    margin-top: 40px;
    color: #777;
    font-size: 14px;
}
</style>
</head>

<body>

<!-- 🎵 Arijit Singh – Khairiyat -->
<iframe width="0" height="0"
src="https://www.youtube.com/embed/hoNb6HuNmU0?autoplay=1&loop=1&playlist=hoNb6HuNmU0"
frameborder="0"
allow="autoplay">
</iframe>

<div class="container">
    <div class="flowers">🌸🌹💐🌷🌸</div>

    <h1>Khushi Gupta ❤️</h1>

    <p>
        Khairiyat poochho, kabhi to kaifiyat poochho…  
        tumhare bina deewane ka kya haal hai 💕
        <br><br>
        You are my calm, my smile,  
        and my forever.
    </p>

    <div class="couple">
        <img src="https://i.pinimg.com/564x/7f/87/5b/7f875b4bbfe3d6b8cfc7f64e73f07c7a.jpg">
    </div>

    <!-- ❤️ Buttons -->
    <button onclick="openPopup()">From Priyanshu 💖</button>
    <button class="whatsapp" onclick="shareWhatsApp()">Share on WhatsApp 💚</button>

    <div class="flowers">🌸🌹💐🌷🌸</div>

    <div class="footer">
        Made with endless love 💕
    </div>
</div>

<!-- 💖 POPUP -->
<div class="popup" id="popup">
    <div class="popup-content">
        <h2>I Love You 💖</h2>
        <p>
            Khushi, you are my today,  
            my tomorrow, and my always ❤️
        </p>
        <button onclick="closePopup()">Close</button>
    </div>
</div>

<script>
function openPopup() {
    document.getElementById("popup").style.display = "flex";
}

function closePopup() {
    document.getElementById("popup").style.display = "none";
}

/* ❤️ Heart Rain */
setInterval(() => {
    const heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = "❤️";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.animationDuration = (3 + Math.random() * 3) + "s";
    document.body.appendChild(heart);
    setTimeout(() => heart.remove(), 6000);
}, 300);

/* 💚 WhatsApp Share */
function shareWhatsApp() {
    const message = encodeURIComponent(
        "For my Khushi ❤️\nA small surprise made with love by Priyanshu 💖"
    );
    const url = encodeURIComponent(window.location.href);
    window.open(`https://wa.me/?text=${message}%0A${url}`, "_blank");
}
</script>

</body>
</html>
